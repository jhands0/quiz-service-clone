# Go + Fiber + MongoDB Backend

The backend for the kahoot clone project.

## Build Setup

``` bash
# install dependencies
$ go mod download

# build for production and launch server
$ go build
$ go run .
```